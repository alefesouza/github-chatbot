import * as builder from 'botbuilder';

const UserInfo: builder.IDialogWaterfallStep = () => {};

const UserInfoResult: builder.IDialogWaterfallStep = () => {};

export default [UserInfo, UserInfoResult];
